using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ImageSoundProcessing.Helpers
{
    public static class Effect
    {
        public static Bitmap Brightness(Bitmap original, float factor)
        {
            Bitmap result = new Bitmap(original);

            return result;
        }

        public static Bitmap Contrast(Bitmap original, float factor)
        {
            Bitmap result = new Bitmap(original);

            return result;
        }

        public static Bitmap Negative(Bitmap original, float factor)
        {
            Bitmap result = new Bitmap(original);

            return result;
        }
    }
}
